# Compiled binary (root-level only, not the cmd/server/ directory)
/server
/server.exe

# SQLite data directory and database files
data/
*.db
*.db.bak

# Local environment — never commit secrets
.env

# Go test binaries
*.test
